# Backend API base URL, baked in at build time. Defaults to /api (relative
# path-based routing on the same domain - see src/api.js) if unset. Set to
# an absolute URL (e.g. https://radiome.orosz.cc) for a deploy without
# path-based routing in front of it.
VITE_API_BASE=/api
